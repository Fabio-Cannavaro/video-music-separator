# video-music-separator 프로젝트 규칙

## 작업 범위와 파일 보호

- `AGENTS.local.md`가 있으면 이 파일 다음에 읽는다. 해당 파일은 개인 작업 메모이므로
  Git으로 추적하거나 공개하지 않는다.
- 작업 전후에 `git status --short`를 확인한다.
- 기존 입력 미디어, 생성 WAV/MP4, AI 모델, 런타임, FFmpeg, 진단 로그,
  백업 EXE와 사용자 변경은 건드리거나 커밋하지 않는다.
- 이번 요청에 필요한 소스, 테스트와 문서만 수정한다.

## 코드 변경 검증

- 변경된 동작에 대응하는 테스트를 추가하거나 수정한다.
- 코드 변경 후 `git diff --check`와
  `.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -v`를 실행한다.
- 앱 동작이나 배포 코드가 바뀌면 테스트 통과 후
  `.\scripts\build_executables.ps1`로 앱 EXE와 설치 EXE를 다시 빌드한다.
- 빌드 성공만으로 완료 처리하지 않는다. 새로 빌드된 루트의
  `video-music-separator.exe`를 직접 실행해 변경된 기능과 핵심 경로를 확인한다.
- 최종 EXE와 설치 EXE의 존재 여부, 크기, 수정 시각과 SHA-256을 확인한다.

## AV-CASS와 CUDA 변경 검증

- AV-CASS, CUDA, 추론 단계, 청크, overlap, OLA 또는 음악 마스크를 변경한 경우
  전체 250단계 추론을 바로 시작하지 않는다.
- 먼저 대표 실제 영상의 1개 청크를 2~4단계로 실행한다.
- 실시간 단계 로그, `nvidia-smi`의 해당 Python GPU 사용률 상승, 음악 WAV와
  비음악 WAV 생성을 모두 확인한 경우에만 전체 250단계 테스트를 실행한다.
- 로그, GPU 사용률과 출력 파일이 일정 시간 변하지 않으면 프로세스가 살아 있다는
  이유만으로 기다리지 않는다. 정확한 PID와 실행 경로를 확인하고 안전하게 종료한
  뒤 Python 스택과 실제 정체 지점을 진단한다.
- 전체 실행 후 추론 단계, overlap, blend mode, high-band extension, 파티션 수,
  WAV 생성, 음악 제거 MP4 내보내기, 재합성 오차와 최종 완료 상태를 확인한다.
- AV-CASS 기본값은 `1초 overlap + cosine-squared OLA + 고역 확장 0%`를 유지한다.
- 분리 품질은 대표 실제 영상의 직접 스템과 음악 제거 영상을 청취해 판단한다.
  차이가 불명확하거나 대사 보존이 검증되지 않은 옵션은 기본값으로 채택하지 않는다.

## 문서와 GitHub 배포 정보

- 사용자에게 보이는 기능, 기본값, 파일명 또는 설치 방식이 바뀌면 README의
  한국어·영어 설명과 GitHub 저장소 About 설명을 함께 업데이트한다.
- 배포 관련 변경은 최신 GitHub 릴리스 설명에도 한국어·영어로 반영한다.
- 저장소 About 설명의 최신 버전, `app/release_info.py`의 `APP_VERSION`, README의
  현재 버전·Release 링크·자산 파일명, 최신 공개 설치 Release의 태그·제목·자산
  파일명은 항상 동일한 버전을 가리켜야 한다. 릴리스 관련 작업의 시작과 종료 시 이
  항목들을 실제 로컬 파일과 GitHub에서 모두 확인하고, 하나라도 다르면 같은 작업에서
  함께 갱신한다.
- GitHub 저장소 About 설명에는 현재 최신 공개 설치 버전을 `vX.Y.Z` 형식으로
  명시한다. 규칙이나 설명만 바뀌어 설치물에 영향이 없는 경우에는 현재 버전을
  유지할 수 있지만, 앱 동작이나 설치물이 바뀌면 새 패치 버전을 발행한다.
- 기존 릴리스 ZIP을 교체하지 않았다면 최신 변경이 그 ZIP에 포함된 것처럼 쓰지
  않고, 소스 상태와 실제 릴리스 자산 상태를 명확히 구분한다.
- 사용자에게 보이는 코드나 설치 방식이 바뀌고 검증이 끝나면 같은 작업에서 정확히
  대응하는 깨끗한 소스 커밋으로 설치 ZIP을 다시 만들고 최신 GitHub Release까지
  갱신한다. 기존 버전의 태그와 자산을 새 소스로 덮어쓰지 않고 패치 버전을 올린다.
- 사용자 변경 때문에 현재 작업 폴더에서 깨끗한 배포본을 만들 수 없으면 변경을
  건드리지 않고 별도의 깨끗한 작업 공간에서 빌드한다. 최신 Release 갱신이 실제로
  막힌 경우에는 소스만 완료된 것처럼 끝내지 않고 정확한 차이와 차단 원인을 알린다.
- 공개 릴리스 직전에는 모델과 FFmpeg의 공식 출처, 라이선스, 다운로드 URL과
  SHA-256을 다시 확인한다.

## 연구진 허가와 외부 구성요소

- 저장소와 최신 설치 테스트용 프리릴리스는 공개 상태지만, AV-CASS
  체크포인트의 자동 다운로드와 일반 사용자 이용 범위에 대한 연구진의 서면 답신은
  아직 대기 중이다. 명시적인 허가와 조건을 받기 전에는 일반 공개 배포가 완료됐다고
  표현하지 않는다.
- AV-CASS 체크포인트는 저장소나 Release 자산에 포함하거나 미러링하지 않는다.
  허가 범위가 확정되기 전까지 설치 프로그램은 공식 프로젝트 링크에서 사용자 PC로
  직접 내려받고 예상 크기와 SHA-256을 검증하는 현재 방식을 유지한다.
- AV-CASS, Diff-Foley/CAVP, FFmpeg, Python 패키지의 코드와 모델 가중치는 각각
  별개의 저작물과 이용조건으로 다룬다. CAVP 또는 Diff-Foley의 라이선스를 근거로
  AV-CASS 체크포인트의 이용조건을 추정하지 않는다.
- 연구진의 답신이 오면 전체 스레드와 첨부를 확인하고, 명시적으로 허용된 범위,
  조건부 요구사항과 아직 답변되지 않은 항목을 구분해 번역·정리한다. 호의적인 표현이나
  일부 답변만으로 상업적 이용, 결과물 이용 또는 재배포까지 허용됐다고 확대 해석하지
  않는다.
- 허가 조건이 현재 구현이나 설명과 다르면 설치 동작, 고지문, README,
  `docs/DISTRIBUTION_CHECKLIST.md`와 최신 GitHub Release 설명을 함께 수정하고 다시
  검증한다.
- 테스트로 확인하지 않은 대사 보존을 주장하지 않는다. 현재 목적과 성능 설명은
  원하지 않는 음악을 줄이면서 효과음을 최대한 보존하는 범위로 한정한다.

## Git 전달과 완료 보고

- 검증된 관련 파일만 `main`에 커밋하고 `origin/main`으로 푸시한다.
- 소유권 경고가 발생하면 전역 설정을 바꾸지 않고 해당 명령에만
  `git -c safe.directory="<현재 저장소의 절대 경로>"`
  옵션을 사용한다.
- 푸시 후 로컬과 원격의 ahead/behind가 `0/0`인지 확인한다.
- 최종 보고에는 테스트 수, 실제 EXE 검증 결과, 적용된 기본값, 커밋 해시,
  푸시 상태와 남은 미검증 항목을 구분해 적는다.
- 소스 테스트, 빌드, EXE 실행, CUDA 작동, 출력 생성과 청취 확인을 서로 구분하며,
  직접 확인하지 않은 사항을 완료했다고 표현하지 않는다.
